
<!DOCTYPE html>
<html>
  <meta charset="utf-8" />
  <body>
       <pre id="canvas">canvas</pre>

     <script src='https://d3js.org/d3.v7.min.js'></script>
      <script>
      // const data = [50, 400, 300, 900, 250, 1000]
      const width = 500;
      const barHeight = 20;
      const margin = 1;
      
      var heartfailurecsv = 'https://raw.githubusercontent.com/rahoofhw/F21DV/ddeea3b245381daca993a645099a570a3b0c83c1/lab1/files/chartdata.csv';
      var scale = d3.scaleLinear().domain([d3.min(data), d3.max(data)]).range([50, 500]);
      var svg = d3.select("body").append("svg").attr("width", width);
      
      d3.csv(heartfailurecsv, function(data,i) {
         var d= data.value;
         // var color = 'blue';
         // if(d>500){color= 'red';} else if(d<100){color= 'green';} 
                  d3.select("#canvas").text(d);

         // svg.attr("height", barHeight * data.length);
         // var g = svg.append("g") .attr("transform", "translate(0," + (i * barHeight) + ")");
       
         // g.append("rect").attr("width",scale(d)).attr("height", barHeight - margin).attr('fill', color);
         // g.append("text").attr("x", scale(d)).attr("y", barHeight / 2)
         // .attr("dy", ".35em").style('text-anchor', 'end').text( d);
      }
      </script>
  </body>
  
</html>




